import 'package:flutter/material.dart';

class SmothiesTab extends StatelessWidget {
  const SmothiesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("SmothiesTab"),
    );
  }
}